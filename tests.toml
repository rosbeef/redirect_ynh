#:schema https://raw.githubusercontent.com/YunoHost/apps/master/schemas/tests.v1.schema.json

test_format = 1.0

[default]
    args.redirect_type = "redirect"
    args.target_servers = "target.domain.tld"
    args.target_protocol = "https"

    preinstall = """
      yunohost domain add target.domain.tld
      yunohost app install https://github.com/YunoHost-Apps/helloworld_ynh -a "domain=target.domain.tld&path=/&init_main_permission=visitors" --force
      # Makes any path serve the index.html page:
      sed -i 's,try_files,try_files index.html,' /etc/nginx/conf.d/target.domain.tld.d/helloworld.conf
      touch /etc/nginx/conf.d/target.domain.tld.d/upstream.conf.inc
      echo '127.0.0.1' > /etc/nginx/conf.d/target.domain.tld.d/upstream.conf.inc
      systemctl reload nginx
"""

    # Turns out 302 redirects cant be made private because they are interpreted before going through the sso ...
    exclude = ["install.private"]

    [default.test_upgrade_from.108632f]
        name = "v2 era"
        args.domain = "domain.tld"
        args.path = "/"
        args.redirect_type = "visible_302"
        args.target = "https://target.domain.tld"
        args.is_public = true
    
        [default.curl_tests]
        home.path = "/"
        home.auto_test_assets = false
    
[passthrough]
    args.redirect_type = "passthrough"
    args.target_servers = "server 127.0.0.1:1234;"
    args.target_protocol = "https"

    exclude = ["install.private", "install.multi", "backup_restore", "change_url"]

    preinstall = """
    cat << EOF > /etc/systemd/system/whatever.service
    
[Unit]
Description=Whatever
After=network.target

[Service]
WorkingDirectory=/redirect/
ExecStart=python3 -m http.server -b 127.0.0.1 1234
PrivateTmp=true

[Install]
WantedBy=multi-user.target
EOF

    mkdir -p /redirect
    mkdir -p /redirect/path
    echo 'helloworld' > /redirect/index.html
    echo 'helloworld' > /redirect/path/index.html
    systemctl daemon-reload
    systemctl enable whatever --now
    true
"""

    preupgrade = """
    cat << EOF > /etc/systemd/system/whatever.service
    
[Unit]
Description=Whatever
After=network.target

[Service]
WorkingDirectory=/redirect/
ExecStart=python3 -m http.server -b 127.0.0.1 1234
PrivateTmp=true

[Install]
WantedBy=multi-user.target
EOF

    mkdir -p /redirect
    mkdir -p /redirect/path
    echo 'helloworld' > /redirect/index.html
    echo 'helloworld' > /redirect/path/index.html
    systemctl daemon-reload
    systemctl enable whatever --now
    true
"""

[passthrough.test_upgrade_from.108632f]
    name = "v1 era"
    args.domain = "domain.tld"
    args.path = "/"
    args.redirect_type = "proxy"
    args.redirect_path = "http://127.0.0.1:1234"
    args.is_public = true
    
[reverseproxy]

    args.redirect_type = "reverseproxy"
    args.target_servers = "server 127.0.0.1:1234;"
    args.target_protocol = "http"

    exclude = ["install.private", "install.multi", "backup_restore", "change_url"]

    preinstall = """
    cat << EOF > /etc/systemd/system/whatever.service

[Unit]
Description=Whatever
After=network.target

[Service]
WorkingDirectory=/redirect/
ExecStart=python3 -m http.server -b 127.0.0.1 1234
PrivateTmp=true

[Install]
WantedBy=multi-user.target
EOF

    mkdir -p /redirect
    mkdir -p /redirect/path
    echo 'helloworld' > /redirect/index.html
    echo 'helloworld' > /redirect/path/index.html
    systemctl daemon-reload
    systemctl enable whatever --now
    true
"""

    preupgrade = """
    cat << EOF > /etc/systemd/system/whatever.service
    
[Unit]
Description=Whatever
After=network.target

[Service]
WorkingDirectory=/redirect/
ExecStart=python3 -m http.server -b 127.0.0.1 1234
PrivateTmp=true

[Install]
WantedBy=multi-user.target
EOF

    mkdir -p /redirect
    mkdir -p /redirect/path
    echo 'helloworld' > /redirect/index.html
    echo 'helloworld' > /redirect/path/index.html
    systemctl daemon-reload
    systemctl enable whatever --now
    true
"""

[reverseproxy.test_upgrade_from.108632f]
    name = "v1 era"
    args.domain = "domain.tld"
    args.path = "/"
    args.redirect_type = "proxy"
    args.redirect_path = "http://127.0.0.1:1234"
    args.is_public = true
